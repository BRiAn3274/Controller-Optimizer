Controller Optimizer 1.7.0: Hardened the mod around an input-only compatibility boundary. Removed all player-laser entity inspection and lifetime mutation; the game now owns every attack entity, damage value, charge, and lifetime. Charge-familiar release handling is limited to original Lil Brimstone and Lil Monstro and only returns release input. Stable per-player identity preserves direction and release history across engine userdata wrappers. Mars leaves held movement input native. Schoolbag quick swap remains enabled by default, requires the actual collectible and two occupied slots, skips ambiguous shared-controller targets, and uses right-stick press. Added defensive optional-API handling and conflict-boundary regression tests while preserving the 1.6.0 Brimstone release, Analog Stick 360-degree aiming, Tainted Azazel one-trigger charge sequence, and snapback protection.
